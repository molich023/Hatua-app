[build]
  command = "npm run build"
  publish = ".next"

# Netlify DB (Neon) integration

[dev]
  command = "npm run dev"
  port = 3000

[functions]
  external_node_modules = ["@neondatabase/serverless"]
